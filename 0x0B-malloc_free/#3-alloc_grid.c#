#include <stdlib.h>
#include "main.h"

/**
 * **alloc_grid - creates
 * @width: width
 * @height: height
 *
 * return: pointer
 */
int **alloc_grid(int width, int height)
{
  int **arr;
  int i, j;

  if (height <= 0  width <= 0)
    return (NULL);
  #
    arr = (int **) malloc(sizeof(int *