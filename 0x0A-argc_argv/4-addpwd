#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>

/**
 * main - adds
 * @argc: number
 * @argv: array
 * Return: 0 - Success
 */

int main(int argc, char *argv[])
{
  int i, j, add = 0;

  for (i = 1; i < argc; i++)
    {
      for (j = 0; argv[i][j] != '\0'; j++)
	{
	  if (!isdigit(argv[i][j]))
	    